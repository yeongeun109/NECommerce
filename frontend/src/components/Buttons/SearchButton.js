import { Button, Icon } from "semantic-ui-react";

const SearchButton = () => {
    return (
        <button>
            search
            <Icon name="search"/>
        </button>
    )
}

export default SearchButton